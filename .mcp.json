{
  "mcpServers": {
    "reddit": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "set -a; [ -f /Users/firegroup/projects/poe/poe2/.env ] && . /Users/firegroup/projects/poe/poe2/.env; set +a; exec npx -y reddit-mcp-server"
      ]
    }
  }
}
